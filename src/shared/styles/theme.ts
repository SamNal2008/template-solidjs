import { createTheme } from "@suid/material/styles";

export const theme = createTheme({
  shape: {
    borderRadius: 8,
  },
  palette: {
    background: {
      paper: "#232222f7",
    },
    mode: "dark",
    primary: {
      main: "#FFFFFF",
    },
    secondary: {
      main: "#425EF9",
    },
  },
});
